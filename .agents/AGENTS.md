# Agent Rules

- **Version Control:** Do NOT automatically commit and push changes. The user prefers to manually commit and push via GitHub Desktop App unless explicitly asked to automate it.
- **Build Verification:** After every code revision, always restart the local dev server running on port 3001 to ensure the latest bundle compiles successfully, is served fresh, and updates the build timestamp visible in the client UI.

